{{#if before}}
  {{#if fieldObj.hasError}}
    {{yield}}
  {{/if}}
{{/if}}

{{#if fieldObj.value}}
  <input id="{{_id}}" type="checkbox" name="{{_name}}" checked>
{{else}}
  <input id="{{_id}}" type="checkbox" name="{{_name}}">
{{/if}}

{{#if after}}
  {{#if fieldObj.hasError}}
    {{yield}}
  {{/if}}
{{/if}}
